// Removed 2026-04-22 — onboarding now infers units from device locale
// rather than reading from Apple Health. Kept as an empty file (can be
// deleted manually) to preserve any git history on the path.
import Foundation
