// Deprecated — merged into Step_Verdict (rank + scan photo + focus areas + plan preview
// all on one screen, now driven by real scan data instead of just form answers).
// Leaving file in place (can't rm) but emptied.
