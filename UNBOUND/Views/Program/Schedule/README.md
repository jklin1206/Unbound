# Program/Schedule

Weekly schedule editor sheet (V4): drag-to-reorder training categories by day, optimize split from program focuses, and set the week phase (heavy/moderate/light/deload).

| File | Purpose |
|------|---------|
| `ProgramScheduleEditorViews.swift` | `WeeklyScheduleEditorSheet` — V4 schedule editor with drag reorder, Optimize Split CTA, and week-phase picker |

## Where to find X

| Task | File |
|------|------|
| Change the schedule editor layout or week-phase picker | `ProgramScheduleEditorViews.swift` |
| Modify the "Optimize Split" logic | `ProgramScheduleEditorViews.swift` (calls deterministic split helper) |
