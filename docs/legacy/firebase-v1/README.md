# Firebase V1 Archive

This folder is historical context only. UNBOUND now uses Supabase plus local stores for active backend behavior; this Firebase V1 code is kept here so it no longer looks like shippable app source.

Archived from active paths:

- `UNBOUND/CloudFunctions/`
- `firestore.rules`

Do not add new behavior here. If a Firebase-era idea is still useful, re-implement it through the current Supabase/services architecture and document the active owner in `docs/ARCHITECTURE.md`.
